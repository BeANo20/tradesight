# MM2 reference icons

Add local, normalized MM2 item icon images here when connecting the conservative template-matching adapter in `src/lib/recognition.ts`.

The app must never download or run unverified recognition models at runtime.
