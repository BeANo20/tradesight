import{describe,it,expect}from"vitest";import{fromRows,find}from"./importer";describe("importer",()=>{it("normalizes aliases and numeric fields",()=>{const x=fromRows([{Item:"Chroma Beachy",Category:"Chromas","Trade status":"Tradable",Value:"1750",Demand:"5",Aliases:"CBeachy, CBeach"}],"2026-08-13");expect(x[0].aliases).toContain("CBeachy");expect(find(x,"cbeach")[0].tradingValue).toBe(1750)})});
